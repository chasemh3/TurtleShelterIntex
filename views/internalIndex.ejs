<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <style>
    /* Reset and base styles */
    body {
        margin: 0;
        font-family: Arial, sans-serif;
        color: #333;
    }
    
    /* Navigation styles */
    nav {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 15px 30px;
        background-color: rgba(0, 0, 0, 0.7);
        position: fixed;
        top: 0;
        width: 100%;
        z-index: 1000;
    }
    
    nav ul {
        list-style: none;
        display: flex;
        gap: 20px;
        margin: 0;
        padding: 0;
    }
    
    nav a {
        text-decoration: none;
        color: white;
        font-weight: bold;
        transition: color 0.3s;
    }
    
    nav a:hover {
        color: #00a99d;
    }
    
    /* Hero section styles */
    .hero {
        height: 100vh;
        background: url('/views/images/turtleVests.jpeg') no-repeat center center/cover;
        display: flex;
        justify-content: center;
        align-items: center;
        text-align: center;
        position: relative;
        color: white;
        padding: 20px;
    }
    
    .hero::after {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.5);
        z-index: 0;
    }
    
    .hero-content {
        position: relative;
        z-index: 1;
        max-width: 600px;
    }
    
    .hero-content p {
        font-size: 1.2rem;
        font-style: italic;
        margin-bottom: 20px;
    }
    
    .hero-content h1 {
        font-size: 3rem;
        margin-bottom: 20px;
    }
    
    .hero-btn {
        display: inline-block;
        padding: 10px 20px;
        font-size: 1rem;
        color: white;
        background-color: #00a99d;
        text-decoration: none;
        border-radius: 5px;
        transition: background-color 0.3s;
    }
    
    .hero-btn:hover {
        background-color: #007f73;
    }
    
    /* Section styles */
    section {
        padding: 50px;
        text-align: center;
        font-size: 1.2rem;
        line-height: 1.6;
    }
    
</style>    
</head>
<body>
    <header class="hero">
        <nav>
            <ul>
                <li><a href="/internalIndex">Home</a></li>
                <li><a href="/volunteerMaintain">Volunteers</a></li>
                <li><a href="/eventMaintain">Events</a></li>
                <li><a href="/userMaintain">Users</a></li>
                <li><a href="/CompletedEvents">Completed Events</a></li>
                <li><a href="/">Log Out</a></li>
            </ul>
        </nav>
        <h1><%= heroText %></h1>
    </header>
    <main>
        <section id="about">
            <h2>About Us</h2>
            <p><%= aboutText %></p>
        </section>
        <section id="projects">
            <h2>Our Projects</h2>
            <div class="project-cards">
                <% projects.forEach(project => { %>
                    <div class="project-card">
                        <img src="<%= project.image %>" alt="<%= project.title %>">
                        <h3><%= project.title %></h3>
                        <p><%= project.description %></p>
                    </div>
                <% }) %>
            </div>
        </section>
    </main>
</body>
</html>
